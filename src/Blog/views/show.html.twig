{% extends 'layout.html.twig' %}

{% block title slug %}

{% block body %}
    <h1>Bienvenue sur l'article {{ slug }}</h1>
        <div class="alert alert-success">
        {{ demo() }}
        </div>

{% endblock %}